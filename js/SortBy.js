/**
 * @param {Array} arr
 * @param {Function} fn
 * @return {Array}
 */
var sortBy = function(arr, fn) {
    
            return sortedArray = arr.sort((a, b) => fn(a) - fn(b))
           
    


};
